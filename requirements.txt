# Email Access Agent Requirements
# Core dependencies for the Langchain-based email access request agent

# Langchain core dependencies
langchain-core>=0.1.0
langchain-openai>=0.0.5
langchain-anthropic>=0.1.0

# LLM providers (choose based on preference)
openai>=1.0.0
anthropic>=0.18.0

# Email handling
# No additional packages needed - uses Python's built-in imaplib and email modules

# Data validation
pydantic>=2.0.0

# Logging and utilities
python-dotenv>=1.0.0

# Optional: For MCP (Model Context Protocol) integration
# mcp>=0.1.0  # Uncomment if using actual MCP client

# Development dependencies (optional)
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
